import { FilterEtudiantPipe } from './filter-etudiant.pipe';

describe('FilterEtudiantPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEtudiantPipe();
    expect(pipe).toBeTruthy();
  });
});
